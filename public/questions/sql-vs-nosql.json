{
  "topic": "SQL vs NoSQL",
  "questions": [
    {
      "q": "A transfer debits one account and credits another in one transaction. Which ACID property ensures neither change applies without the other?",
      "options": [
        "Isolation, which orders the two writes so readers never interleave them",
        "Atomicity, which commits or rolls back the pair as a single unit",
        "Durability, which persists both changes to disk before acknowledging",
        "Consistency, which validates the account balances after each write"
      ],
      "answer": 1,
      "difficulty": "easy",
      "explanation": "Atomicity is the all-or-nothing guarantee: the debit and credit either both commit or both roll back. Isolation governs concurrent visibility and durability governs survival after commit."
    },
    {
      "q": "Which workload most clearly favors a relational database over a key-value store?",
      "options": [
        "Ad-hoc reports joining orders, customers, and inventory tables",
        "Looking up session payloads by their unique session token",
        "Caching rendered page fragments keyed by their URL",
        "Storing device telemetry appended at very high rates"
      ],
      "answer": 0,
      "difficulty": "easy",
      "explanation": "Ad-hoc multi-table joins are exactly what a query planner and relational model exist for. The other three are single-key access patterns a key-value store handles well."
    },
    {
      "q": "In an eventually consistent store, what does the system actually promise about replicas?",
      "options": [
        "Each replica acknowledges every write before any read gets served",
        "Reads always reflect the most recent successful write",
        "Given no new writes, all replicas converge to the same value",
        "Conflicting writes are impossible because replicas take turns"
      ],
      "answer": 2,
      "difficulty": "easy",
      "explanation": "Eventual consistency promises only convergence once writes stop; it says nothing about any individual read being current. Reading the latest write is the stronger strong-consistency guarantee."
    },
    {
      "q": "Which NoSQL family is purpose-built for traversing relationships like 'friends of friends'?",
      "options": [
        "Wide-column stores, which group related columns into families",
        "Document stores, which nest related records inside a single document",
        "Key-value stores, which link records through composite keys",
        "Graph databases, storing nodes and edges as first-class data"
      ],
      "answer": 3,
      "difficulty": "easy",
      "explanation": "Graph engines store edges as direct pointers, so hopping between connected nodes costs roughly constant time per hop. Other families would need repeated lookups or joins per level."
    },
    {
      "q": "Why do self-contained documents make horizontal write scaling simpler than normalized relational rows?",
      "options": [
        "A document shards as one unit, avoiding cross-node joins on reads and writes",
        "JSON serializes faster than the relational wire protocols used by SQL databases",
        "Documents compress better, so each shard holds proportionally more data",
        "Document stores buffer writes in memory while relational engines cannot"
      ],
      "answer": 0,
      "difficulty": "easy",
      "explanation": "Keeping an aggregate in one document means each operation touches a single shard, so no distributed join or cross-node transaction is needed. Normalized rows scatter one entity across many shards."
    },
    {
      "q": "What does 'schema-on-write' mean in practice for a relational table?",
      "options": [
        "The schema is versioned automatically so old rows keep their original column layout forever",
        "Readers must supply a schema to interpret the raw stored bytes",
        "Inserts violating declared column types or constraints are rejected immediately",
        "The schema can only change by rebuilding the table from scratch"
      ],
      "answer": 2,
      "difficulty": "easy",
      "explanation": "The schema is enforced at insert time, so bad data is rejected up front and every stored row conforms. Schema-on-read, the alternative, defers interpretation to query time."
    },
    {
      "q": "A cart service needs microsecond lookups by session id and no other query patterns. Best fit?",
      "options": [
        "A fully normalized relational schema with the cart split across five joined tables",
        "An in-memory key-value store mapping session id to the cart blob",
        "A document store with secondary indexes on every cart field",
        "A graph database linking session nodes to product nodes"
      ],
      "answer": 1,
      "difficulty": "easy",
      "explanation": "A single known key and no query variety is the textbook key-value case, and in-memory storage delivers the microsecond target. Joins or extra indexes would add cost for capability never used."
    },
    {
      "q": "To stay writable during network partitions, Dynamo-style stores historically relaxed which guarantee?",
      "options": [
        "Durability, acknowledging writes before replicating them anywhere",
        "Client authentication during the partition window",
        "Per-key write atomicity on the accepting replica",
        "Strong consistency, letting replicas briefly diverge and reconcile later"
      ],
      "answer": 3,
      "difficulty": "easy",
      "explanation": "CAP forces a choice under partition, and AP systems keep accepting writes while replicas diverge, reconciling afterward via vector clocks or last-write-wins. Durability and per-key atomicity are retained."
    },
    {
      "q": "An order flow must atomically update accounts, ledger, and audit tables. Why does a basic key-value store struggle?",
      "options": [
        "Key-value stores cannot represent monetary values without floating point error",
        "Each table would need its own separate cluster, tripling operational cost",
        "It offers atomicity per key, but no transaction spanning arbitrary keys",
        "Its eventual durability means committed writes may vanish after a crash"
      ],
      "answer": 2,
      "difficulty": "medium",
      "explanation": "Basic key-value stores guarantee atomicity only within a single key, so a partial failure can leave the ledger updated but not the audit record. Multi-key consistency then has to be built with sagas or compensations."
    },
    {
      "q": "Fields on your user profile change every sprint, and reads are by primary key. What does a document store buy you here?",
      "options": [
        "New optional fields ship without a coordinated table migration",
        "Old documents are rewritten automatically to match the newest shape",
        "Reads become transactional across all documents in the collection",
        "Field renames propagate to historical documents at query time"
      ],
      "answer": 0,
      "difficulty": "medium",
      "explanation": "Flexible schemas let each document carry its own fields, so adding one needs no ALTER TABLE or backfill. The tradeoff is that application code must handle the mix of old and new shapes."
    },
    {
      "q": "An analytics job scans two columns across billions of rows. Why does columnar storage beat row storage here?",
      "options": [
        "Columnar engines cache the entire table in RAM between queries",
        "Row stores must lock each row as it is scanned, throttling reads",
        "Columnar files skip the transaction log entirely, roughly halving total write amplification",
        "Only the two needed columns are read, and similar values compress tightly"
      ],
      "answer": 3,
      "difficulty": "medium",
      "explanation": "Column layout lets the scan read only the requested columns, and homogeneous values compress far better, slashing I/O. A row store would pull every column of every row off disk."
    },
    {
      "q": "Why do relational joins degrade sharply once tables are sharded across many machines?",
      "options": [
        "The SQL standard removes join support once a table is partitioned",
        "Matching rows can live on different nodes, forcing large cross-network shuffles",
        "Sharding invalidates secondary indexes, so every join falls back to full table scans",
        "Join buffers are pinned to one primary node and cannot grow larger"
      ],
      "answer": 1,
      "difficulty": "medium",
      "explanation": "A join across shards must move matching rows over the network to be paired, so cost scales with data shuffled rather than rows returned. Co-locating related rows on the same shard is the usual mitigation."
    },
    {
      "q": "Seconds after you update your bio, a friend still sees the old one. In an eventually consistent store, what most likely happened?",
      "options": [
        "Their read hit a replica the write had not yet reached",
        "The write failed a validation rule and was silently rolled back",
        "A row-level lock made their read return the column's default value",
        "Read-repair deleted the newer value while reconciling replicas"
      ],
      "answer": 0,
      "difficulty": "medium",
      "explanation": "Replication lag means a read served by a not-yet-updated replica returns stale data, which is the normal behavior of such a system. Read-repair pushes newer values out; it does not delete them."
    },
    {
      "q": "When does denormalizing shared data into each document backfire the most?",
      "options": [
        "When documents are usually read in their entirety by the application",
        "When the collection is sharded across more than a handful of nodes",
        "When reads outnumber writes by several orders of magnitude",
        "When many documents copy a value that changes frequently"
      ],
      "answer": 3,
      "difficulty": "medium",
      "explanation": "Every change to a widely duplicated value forces a fan-out update across all copies, and any missed one leaves inconsistent data. Denormalization pays off precisely when the copied data is stable and read-heavy."
    },
    {
      "q": "A team with 5 GB of data and heavy reporting adopts a key-value store 'for scale'. Most likely outcome?",
      "options": [
        "Single-key reads become slower than relational primary-key lookups were",
        "Joins and aggregations get re-implemented, badly, in application code",
        "The store's strict schema checks reject most of their existing records",
        "Write throughput drops because all writes serialize on one global lock"
      ],
      "answer": 1,
      "difficulty": "medium",
      "explanation": "At 5 GB there is no scale problem to solve, so the team loses the query engine and rebuilds joins and aggregates by hand, slower and buggier. Key-value stores also do not impose strict schemas."
    },
    {
      "q": "Which access pattern most justifies a graph database over SQL join tables?",
      "options": [
        "Fetching a user's direct friends list each time their profile page renders",
        "Counting the total number of edges in the network for a daily report",
        "Variable-depth traversals such as shortest path between two members",
        "Sorting all users by signup date with pagination"
      ],
      "answer": 2,
      "difficulty": "medium",
      "explanation": "Unbounded-depth traversal would need one self-join per hop in SQL, while a graph engine walks edge pointers directly. One-hop lookups and aggregate counts are fine in a relational table."
    },
    {
      "q": "A global app needs sub-50 ms reads everywhere; payments need strict ordering; minor feed staleness is fine. Sound architecture?",
      "options": [
        "One strongly consistent global database serving every workload from a single region",
        "Everything eventually consistent, reconciling payment order in a nightly batch",
        "Per-region relational primaries with clients writing to whichever is nearest",
        "Geo-replicated eventual store for feeds, payments routed to one strongly consistent service"
      ],
      "answer": 3,
      "difficulty": "hard",
      "explanation": "Matching each workload to its real consistency need gives local low-latency feed reads while payments keep strict ordering in one authoritative service. A single global primary cannot meet 50 ms worldwide."
    },
    {
      "q": "Which statement best captures transactions in modern NoSQL systems?",
      "options": [
        "Multi-document transactions remain impossible because BASE semantics forbid atomic commits entirely",
        "Several offer multi-document ACID transactions, at a real throughput and latency cost",
        "Transactions are now free because they run entirely on the client",
        "Only graph databases gained transactions, since edges require them"
      ],
      "answer": 1,
      "difficulty": "hard",
      "explanation": "MongoDB, DynamoDB, and others now support multi-record ACID transactions, but the coordination they require costs latency and throughput. The old absolute split between ACID and BASE no longer holds."
    },
    {
      "q": "Postgres handles 5k writes/s at 60% CPU; load doubles yearly. Strongest argument to defer a Cassandra migration now?",
      "options": [
        "Vertical headroom plus read replicas likely covers years, at far lower risk than migrating",
        "Cassandra clusters cannot exceed the write rate of a tuned Postgres primary",
        "Doubling load will halve CPU usage as caches warm over time",
        "Postgres will scale writes automatically once partitioned tables are enabled"
      ],
      "answer": 0,
      "difficulty": "hard",
      "explanation": "Bigger hardware, tuning, and replicas buy several doublings of runway at a fraction of the risk and effort of rewriting the data layer. Migrate when a concrete ceiling is in sight, not preemptively."
    },
    {
      "q": "Why are Cassandra-style data models designed starting from the queries rather than the entities?",
      "options": [
        "CQL lacks a general WHERE clause, so most filtering must happen in the application layer",
        "Entities cannot be represented because storage is a flat log of writes",
        "Each query should hit one partition; cross-partition scans are slow and unbounded",
        "The engine enforces third normal form, constraining table design"
      ],
      "answer": 2,
      "difficulty": "hard",
      "explanation": "Performance depends on the partition key routing a query to a single node, so tables are shaped per query, often duplicating data across several tables. Scatter-gather scans across partitions do not scale."
    }
  ]
}
