{
  "topic": "Message queues & pub/sub",
  "questions": [
    {
      "q": "What does at-least-once delivery guarantee?",
      "options": [
        "Every message is delivered exactly one time to exactly one consumer",
        "Every message is eventually delivered, though consumers may see duplicates",
        "Messages may be dropped, but any delivered message arrives only once",
        "Messages arrive in the exact order producers originally sent them"
      ],
      "answer": 1,
      "difficulty": "easy",
      "explanation": "At-least-once trades duplicates for durability: the broker redelivers until acked, so nothing is lost but a message can arrive twice. Consumers must therefore be idempotent."
    },
    {
      "q": "In pub/sub, what happens when a message is published to a topic?",
      "options": [
        "It goes to a single subscriber selected round-robin for fairness",
        "It is stored until one subscriber explicitly requests and removes it",
        "It is routed only to the subscriber that has been idle the longest",
        "Every subscription attached to the topic receives its own copy"
      ],
      "answer": 3,
      "difficulty": "easy",
      "explanation": "Pub/sub is fan-out: each subscription gets an independent copy of the message. Round-robin to one consumer is queue semantics, not topic semantics."
    },
    {
      "q": "What is the purpose of a consumer group (as in Kafka)?",
      "options": [
        "To share a topic's partitions among members so each message is handled once per group",
        "To duplicate every message to every member so the group tolerates individual crashes",
        "To batch acknowledgements from many consumers into one network round trip",
        "To encrypt traffic between the broker and a trusted set of consumers"
      ],
      "answer": 0,
      "difficulty": "easy",
      "explanation": "A consumer group divides partitions among its members so the group collectively processes each message once, giving horizontal scale. Fan-out happens across groups, not within one."
    },
    {
      "q": "What is a dead-letter queue (DLQ) for?",
      "options": [
        "Holding messages whose TTL expired before any consumer came online",
        "Archiving every successfully processed message for compliance audits",
        "Parking messages that repeatedly failed processing for later inspection",
        "Buffering messages during broker restarts so none are dropped"
      ],
      "answer": 2,
      "difficulty": "easy",
      "explanation": "A DLQ isolates messages that exhausted their retries so they stop blocking the main flow and can be inspected or replayed after a fix."
    },
    {
      "q": "What is the core difference between a queue and a pub/sub topic?",
      "options": [
        "Queues always preserve strict global ordering while topics deliver messages in arbitrary order",
        "A queue delivers each message to one consumer; a topic fans out to many subscribers",
        "Topics persist messages to disk while queues are strictly memory-only",
        "Queues scale horizontally while topics are limited to a single broker"
      ],
      "answer": 1,
      "difficulty": "easy",
      "explanation": "The distinction is cardinality of delivery: queues distribute work one-consumer-per-message, topics broadcast the same event to every subscriber. Persistence and scaling are orthogonal to both."
    },
    {
      "q": "Why do brokers require consumer acknowledgements?",
      "options": [
        "So a message can be redelivered if the consumer dies before finishing it",
        "So the broker can bill producers accurately for each delivered message",
        "So consumers can veto messages they consider malformed or hostile",
        "So the broker knows when to compress older segments of the log"
      ],
      "answer": 0,
      "difficulty": "easy",
      "explanation": "An ack signals that processing actually completed. Without one the broker assumes failure and redelivers, which is what makes at-least-once delivery possible."
    },
    {
      "q": "What does backpressure mean in a messaging pipeline?",
      "options": [
        "Consumers replay old messages backward to rebuild corrupted state",
        "The broker transparently compresses queued messages whenever its memory begins running low",
        "Producers retry failed sends with exponentially growing delays",
        "Downstream slowness propagates upstream, forcing producers to slow or buffer"
      ],
      "answer": 3,
      "difficulty": "easy",
      "explanation": "Backpressure is the feedback signal that a slow consumer sends upstream, throttling producers instead of letting queues grow unbounded until memory or disk fails."
    },
    {
      "q": "Where is ordering typically guaranteed in Kafka-style logs?",
      "options": [
        "Across the whole topic regardless of how many partitions it has",
        "Across all topics owned by the same producer application",
        "Within a single partition, for messages sharing that partition",
        "Nowhere; consumers must reorder using timestamps in each message"
      ],
      "answer": 2,
      "difficulty": "easy",
      "explanation": "Each partition is an independent append-only log with its own offsets, so ordering holds inside a partition only. Partitions are consumed in parallel, so topic-wide order does not exist."
    },
    {
      "q": "A consumer crashes after processing but before acking. With at-least-once delivery, what happens?",
      "options": [
        "The message is redelivered and gets processed a second time",
        "The broker detects completion via a heartbeat and drops the message",
        "The message routes straight to the DLQ as a poisoned message",
        "The whole partition pauses until the crashed consumer rejoins"
      ],
      "answer": 0,
      "difficulty": "medium",
      "explanation": "The broker only knows about acks, not side effects, so an unacked message is redelivered and the work repeats. This is exactly why handlers must be idempotent."
    },
    {
      "q": "Orders for each customer must be processed in sequence, with high throughput overall. Best approach?",
      "options": [
        "Use a single partition for the entire topic to enforce global order",
        "Attach sequence numbers and have every consumer buffer and reorder events indefinitely",
        "Partition by customer ID so each customer's events stay in one ordered stream",
        "Timestamp each event and rely on the broker to sort before delivery"
      ],
      "answer": 2,
      "difficulty": "medium",
      "explanation": "Keying by customer ID gives per-key ordering inside one partition while different customers spread across partitions, so you get sequence where it matters plus parallelism. A single partition would cap throughput."
    },
    {
      "q": "One member of a Kafka consumer group is much slower than the rest. What is the effect?",
      "options": [
        "The broker shrinks its partition count until the slow member recovers",
        "Other members steal individual messages from the slow member's partitions",
        "The group coordinator throttles fast members so all progress evenly",
        "Lag builds on its partitions while other members' partitions stay current"
      ],
      "answer": 3,
      "difficulty": "medium",
      "explanation": "Partitions are exclusively assigned, so a slow member's partitions simply accumulate lag; peers cannot help because they are not allowed to read those partitions."
    },
    {
      "q": "A message keeps failing and being redelivered forever (a poison pill). What is the standard remedy?",
      "options": [
        "Lower the broker's replication factor so the message expires faster",
        "Cap retry attempts, then route the message to a dead-letter queue",
        "Skip acknowledgements entirely so the broker stops redelivering it",
        "Restart the consumer group so offsets reset past the bad message"
      ],
      "answer": 1,
      "difficulty": "medium",
      "explanation": "A bounded retry count plus DLQ routing removes the bad message from the hot path while preserving it for diagnosis, so the rest of the stream keeps flowing."
    },
    {
      "q": "Why can an exactly-once delivery claim be misleading?",
      "options": [
        "Brokers cannot deduplicate anything because unique message identifiers get stripped during transit",
        "It requires perfectly synchronized clocks, which no realistic distributed system can provide",
        "It typically means effectively-once via dedup or transactions, not zero duplicates on the wire",
        "It only works when there is exactly one producer and one consumer"
      ],
      "answer": 2,
      "difficulty": "medium",
      "explanation": "Network retries make duplicate transmission unavoidable; systems achieve exactly-once processing by deduplicating with sequence numbers or committing offsets and output atomically."
    },
    {
      "q": "Producers outpace consumers for hours and queue depth keeps growing. Which mitigation attacks the root cause?",
      "options": [
        "Raise the broker's disk quota so the backlog can grow for longer",
        "Shorten message TTLs so old work silently expires from the queue",
        "Enable gzip compression so the backlog fits in less memory",
        "Scale out consumers or shed load at the producer side"
      ],
      "answer": 3,
      "difficulty": "medium",
      "explanation": "The root cause is a throughput imbalance, so you must raise consumption capacity or reduce ingress. More disk or compression only buys time before the same limit is hit."
    },
    {
      "q": "Why prefer log-based brokers (Kafka) over classic queues for event sourcing and replay?",
      "options": [
        "Consumers keep offsets into a retained log, so history can be re-read at will",
        "Log brokers push messages faster because they skip consumer acknowledgements",
        "Classic queues cannot fan a message out to more than one consumer",
        "Log brokers guarantee global ordering across every partition and topic"
      ],
      "answer": 0,
      "difficulty": "medium",
      "explanation": "A log retains messages after consumption, and reading is just an offset, so a consumer can rewind and rebuild state. Classic queues delete on ack, making replay impossible."
    },
    {
      "q": "Twelve services need the same order events. Why publish once to a topic rather than send twelve queue messages?",
      "options": [
        "Queues would reorder the events differently for every downstream team",
        "Producers stay decoupled from the subscriber list, which can change freely",
        "Topic delivery is guaranteed exactly-once while queue delivery is not",
        "A topic lets subscribers share one consumer offset, halving broker storage"
      ],
      "answer": 1,
      "difficulty": "medium",
      "explanation": "With a topic the producer publishes one event and never learns who consumes it, so subscribers can be added or removed without redeploying the producer."
    },
    {
      "q": "Frequent consumer restarts cause repeated Kafka rebalances. What is the impact and mitigation?",
      "options": [
        "Every in-flight message is permanently lost during each rebalance; enabling producer idempotence recovers them",
        "Offsets reset to zero each time; shrink retention so replay stays cheap",
        "The broker leader flaps; move the cluster to a single beefier broker",
        "Processing pauses during reassignment; use cooperative rebalancing and static membership"
      ],
      "answer": 3,
      "difficulty": "hard",
      "explanation": "Eager rebalancing stops the world while partitions are reassigned. Cooperative (incremental) rebalancing moves only affected partitions, and static membership keeps IDs stable across brief restarts."
    },
    {
      "q": "You need strict ordering AND horizontal scale for a payments stream. What is the realistic design?",
      "options": [
        "Global FIFO across the entire topic, achieved by routing all traffic through a single partition and consumer",
        "Per-key ordering: partition by account so each account is sequential while accounts run in parallel",
        "Broker-side reordering driven by synchronized timestamps across all producers",
        "Consumer-side sorting with a five-minute buffering window before applying events"
      ],
      "answer": 1,
      "difficulty": "hard",
      "explanation": "Global ordering and parallelism are fundamentally at odds, so real systems narrow the ordering requirement to a key. Per-account sequencing is what correctness actually needs, and accounts scale independently."
    },
    {
      "q": "An at-least-once consumer writes to a non-idempotent downstream API. What is the safest pattern?",
      "options": [
        "Deduplicate with a persisted processed-ID store checked before each downstream call",
        "Switch the subscription to at-most-once delivery so duplicate messages become impossible",
        "Retry the downstream call until it succeeds, then ack the message twice",
        "Batch downstream calls so duplicates land in the same HTTP request"
      ],
      "answer": 0,
      "difficulty": "hard",
      "explanation": "Recording processed message IDs durably lets the consumer skip repeats, adding idempotence in front of an API that lacks it. At-most-once would avoid duplicates only by risking lost payments."
    },
    {
      "q": "A broker cluster sits at 80% disk, consumers lag by hours, and producers spike 3x for a day. What likely fails first?",
      "options": [
        "Consumer group coordination collapses entirely because member heartbeats begin timing out under load",
        "Producer batching stops working, tripling the request rate to the broker",
        "Retention or disk limits are hit, forcing eviction or producer throttling before lag clears",
        "Partition leaders migrate continuously, halving the cluster's throughput"
      ],
      "answer": 2,
      "difficulty": "hard",
      "explanation": "Lag means data must sit on disk longer while a 3x spike writes faster, so the finite disk or retention window is the binding constraint: segments get evicted or producers are throttled."
    }
  ]
}
