{
  "topic": "Time-series & high-write workloads",
  "questions": [
    {
      "q": "Why do LSM-tree storage engines handle heavy write loads well?",
      "options": [
        "Writes go to memory and an append-only log, deferring sorted-file merges",
        "They update rows in place, avoiding any write amplification",
        "They automatically shard every incoming write across all disks in the machine",
        "They skip the write-ahead log, halving the I/O per insert"
      ],
      "answer": 0,
      "difficulty": "easy",
      "explanation": "LSM trees turn random writes into sequential ones: data lands in an in-memory memtable plus append-only WAL, and sorting into SSTables happens later during background compaction."
    },
    {
      "q": "Compared with LSM trees, what is a B-tree engine's typical strength?",
      "options": [
        "Much faster sustained ingestion of random writes at very high data volumes",
        "Lower disk usage because pages never fragment",
        "Efficient point reads and range scans without merging multiple files",
        "No need for background maintenance processes"
      ],
      "answer": 2,
      "difficulty": "easy",
      "explanation": "A B-tree keeps one sorted structure, so a lookup or range scan follows a single path. LSM reads may have to check several SSTable levels and merge results."
    },
    {
      "q": "What is downsampling in a time-series database?",
      "options": [
        "Dropping every record older than the retention window immediately",
        "Replacing old raw points with lower-resolution aggregates like 5-minute rollups",
        "Sampling a fraction of incoming writes to reduce the ingest load",
        "Compressing timestamps by storing deltas instead of absolutes"
      ],
      "answer": 1,
      "difficulty": "easy",
      "explanation": "Downsampling aggregates older raw points into coarser buckets, keeping long-term trends queryable while shrinking storage. It reduces resolution, not the ingest rate."
    },
    {
      "q": "What does a retention policy in a time-series store control?",
      "options": [
        "Which users may query each measurement and tag",
        "How database replicas resynchronize after a temporary network partition",
        "The maximum cardinality allowed per tag key",
        "How long data is kept before automatic expiry or deletion"
      ],
      "answer": 3,
      "difficulty": "easy",
      "explanation": "A retention policy is a TTL on data age: once points pass the configured window the database expires them automatically, bounding disk growth without manual cleanup."
    },
    {
      "q": "In time-series data models, what is 'tag cardinality'?",
      "options": [
        "The number of data points stored per series each second",
        "The number of distinct values a tag key can take across series",
        "The maximum count of tag keys permitted on any single measurement",
        "The ratio of tags to fields in each written point"
      ],
      "answer": 1,
      "difficulty": "easy",
      "explanation": "Cardinality counts distinct tag values. Since each unique tag combination defines its own series, high cardinality multiplies series count and index size."
    },
    {
      "q": "Why are time-series writes considered append-friendly?",
      "options": [
        "Points are keyed randomly, spreading writes evenly across pages",
        "Old points are rewritten each time a newer point arrives",
        "Each incoming write updates an aggregate row in place",
        "New points mostly carry recent timestamps, extending the series' end"
      ],
      "answer": 3,
      "difficulty": "easy",
      "explanation": "Timestamps arrive in roughly increasing order, so writes append to the tail of each series rather than mutating older data, giving sequential I/O and immutable history."
    },
    {
      "q": "What is a common first-line technique to survive write spikes to a time-series store?",
      "options": [
        "Synchronous writes straight to the storage nodes for durability",
        "Read replicas that absorb the extra write traffic",
        "Buffering through a message queue that smooths bursts",
        "Doubling the retention period so data spreads over more disk"
      ],
      "answer": 2,
      "difficulty": "easy",
      "explanation": "A queue like Kafka absorbs the burst and lets consumers drain at a steady rate the database can sustain. Read replicas do not help, since they take no writes."
    },
    {
      "q": "Which compression trick exploits the shape of typical metric data?",
      "options": [
        "Delta-of-delta timestamps and XOR-encoded float values",
        "Dictionary-encoding each measurement name per data point",
        "Encrypting repeated values so they deduplicate in storage",
        "Row-level gzip applied independently to every point"
      ],
      "answer": 0,
      "difficulty": "easy",
      "explanation": "The Gorilla scheme exploits regular scrape intervals and slowly changing values: delta-of-delta timestamps collapse to near zero bits and XORed floats share most leading bits."
    },
    {
      "q": "Ingest slows and read latency spikes while background disk I/O is saturated on an LSM store. What is the likely cause?",
      "options": [
        "The WAL is replaying because a replica restarted",
        "B-tree page splits are cascading through the index",
        "Compaction is falling behind, piling up SSTables and read amplification",
        "The block cache keeps evicting hot pages because of large scans"
      ],
      "answer": 2,
      "difficulty": "medium",
      "explanation": "When compaction cannot keep pace, SSTables accumulate so reads must check more files, and the engine eventually throttles or stalls writes to let merging catch up."
    },
    {
      "q": "You add request_id as a tag on every metric point. Weeks later the TSDB's memory explodes. Why?",
      "options": [
        "Each unique tag value creates a new series, and series indexes live in memory",
        "Tag values are stored uncompressed on disk, roughly doubling the footprint of every point",
        "request_id values collide across shards, forcing constant rebalancing",
        "High-frequency writes bypass the buffer and fragment the WAL"
      ],
      "answer": 0,
      "difficulty": "medium",
      "explanation": "A unique per-request tag is a classic cardinality bomb: every request spawns a distinct series, and the in-memory inverted index over series keys grows without bound."
    },
    {
      "q": "Partitioning a metrics table by metric_name sends 40% of traffic to one node. Which repartitioning usually fixes this?",
      "options": [
        "Partition by ingestion server hostname instead of the metric",
        "Partition by metric_name plus a random suffix on every write",
        "Move the hot metric to its own dedicated cluster permanently",
        "Partition by a composite of series key and time bucket"
      ],
      "answer": 3,
      "difficulty": "medium",
      "explanation": "Hashing the full series key spreads the hot metric's many series across nodes, while the time bucket keeps each partition scannable. A random suffix would scatter reads unusably."
    },
    {
      "q": "Queries for 'last 5 minutes' dashboards are fast, but 90-day trend queries crush the cluster. Which design addresses the trend queries?",
      "options": [
        "Increase scrape frequency so trends need fewer points",
        "Precomputed rollups at hourly resolution queried instead of raw data",
        "A much larger block cache dedicated to holding raw historical blocks",
        "Query-time sampling that reads every tenth raw point"
      ],
      "answer": 1,
      "difficulty": "medium",
      "explanation": "Aggregating once at write time turns a 90-day scan of billions of raw points into a read of a few thousand hourly buckets, and trend charts do not need raw resolution."
    },
    {
      "q": "Why does time-based partitioning (one partition per day) make retention cheap?",
      "options": [
        "Old rows compress better once the partition is a day old",
        "Queries never touch old partitions, so deletes are invisible",
        "Each daily partition maintains its own write-ahead log that expires automatically",
        "Expiry becomes dropping whole partitions instead of row-by-row deletes"
      ],
      "answer": 3,
      "difficulty": "medium",
      "explanation": "Dropping a partition is a metadata operation that frees whole files at once, avoiding the per-row delete traffic, tombstones, and compaction churn of aging data out individually."
    },
    {
      "q": "A write-heavy IoT platform sees 10x write amplification on a B-tree store. What mostly drives that amplification?",
      "options": [
        "Foreign-key checks executed for every single inserted reading",
        "Random-key inserts dirtying and rewriting many pages plus index updates",
        "Compaction rewriting SSTables at each level of the tree",
        "Timestamps stored as strings inflating each row's size"
      ],
      "answer": 1,
      "difficulty": "medium",
      "explanation": "Each small insert dirties a whole page, and scattered keys touch many pages plus secondary indexes, so the engine flushes far more bytes than the rows contain. Compaction belongs to LSM, not B-trees."
    },
    {
      "q": "Dashboard queries using regex over metric names are slow at high series counts. What's the underlying reason?",
      "options": [
        "Regex matching must scan the series index rather than seek exact keys",
        "Regex forces the store to decompress every data block fully",
        "The query planner serializes all regex queries onto one thread",
        "Metric names are hashed, so regex requires rebuilding names from hashes"
      ],
      "answer": 0,
      "difficulty": "medium",
      "explanation": "An exact label match is an index lookup, but a regex must be tested against every candidate name, so cost grows linearly with the number of series in the index."
    },
    {
      "q": "You must keep raw data 7 days, 1-minute rollups 90 days, hourly rollups 2 years. What implements this cleanly?",
      "options": [
        "One table with a TTL column checked by every query",
        "Client-side filtering that ignores data past each horizon",
        "Tiered retention policies with continuous or scheduled aggregation jobs",
        "Nightly exports of old raw data into CSV archives kept on object storage"
      ],
      "answer": 2,
      "difficulty": "medium",
      "explanation": "Each resolution lives in its own store with its own TTL, fed by continuous aggregation, so expiry is automatic per tier and queries route to the right resolution."
    },
    {
      "q": "An LSM store shows fast writes but p99 reads degrade over weeks. Compaction is keeping up. What subtler cause fits?",
      "options": [
        "The WAL has grown past the operating system's page cache size",
        "Bloom filters and indexes for many levels no longer fit in memory",
        "Tombstones from TTL deletes are pinned in level zero forever",
        "Timestamp compression breaks once epochs exceed 32 bits"
      ],
      "answer": 1,
      "difficulty": "hard",
      "explanation": "As the dataset grows, bloom filters and block indexes spill out of RAM, so each lookup adds disk seeks just to decide which SSTables to read, inflating tail latency."
    },
    {
      "q": "Sizing: 200k devices send one 100-byte point every 10 seconds. Roughly what sustained write rate must the cluster absorb?",
      "options": [
        "About 20k points/sec, roughly 2 MB/sec before replication",
        "About 2k points/sec, roughly 200 KB/sec before replication",
        "About 200k points/sec, roughly 20 MB/sec before replication",
        "About 1.2M points/sec, roughly 120 MB/sec before replication"
      ],
      "answer": 0,
      "difficulty": "hard",
      "explanation": "200,000 divided by 10 seconds is 20k points/sec, and 20k times 100 bytes is 2 MB/sec. Option C wrongly assumes one point per device per second."
    },
    {
      "q": "A multi-tenant TSDB lets one tenant register unbounded tag values, degrading everyone. Which control is most effective?",
      "options": [
        "Per-tenant rate limits on write requests per second",
        "Per-tenant disk usage quotas enforced strictly at the storage layer",
        "Per-tenant series-cardinality limits enforced at ingestion",
        "Per-tenant query timeouts that kill expensive scans"
      ],
      "answer": 2,
      "difficulty": "hard",
      "explanation": "The damage is series-index growth, so the limit must be on distinct series and rejected at write time. A slow trickle of unique tags still explodes cardinality under a rate limit."
    },
    {
      "q": "Writes arrive mostly in time order, but 2% are days late (device backfill). Why do very late writes hurt LSM/TSDB performance disproportionately?",
      "options": [
        "Late points must be encrypted separately from their block",
        "The WAL must be replayed from the late point's timestamp forward",
        "Late points force the query planner to disable index usage",
        "They land in already-compacted or sealed time blocks, forcing rewrites or overlaps"
      ],
      "answer": 3,
      "difficulty": "hard",
      "explanation": "Out-of-order points reopen or duplicate historical blocks, triggering extra compaction and creating overlapping time ranges that reads must merge, so a tiny fraction costs heavily."
    }
  ]
}
