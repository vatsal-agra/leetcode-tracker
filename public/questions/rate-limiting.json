{
  "topic": "Rate limiting",
  "questions": [
    {
      "q": "In a token bucket limiter, what does the bucket capacity control?",
      "options": [
        "The steady-state request rate allowed over long time periods",
        "How long a client must wait after a single rejected request",
        "The number of distinct clients a single bucket can track",
        "The maximum burst size allowed above the steady refill rate"
      ],
      "answer": 3,
      "difficulty": "easy",
      "explanation": "Capacity is how many tokens can accumulate while a client is idle, so it bounds the burst that can be spent at once. The refill rate, not capacity, sets the long-run average."
    },
    {
      "q": "In a token bucket, what does the refill rate correspond to?",
      "options": [
        "The sustained average request rate permitted over time",
        "The maximum instantaneous burst the limiter will accept",
        "The interval between full resets of every client's bucket",
        "The penalty duration applied to clients exceeding quota"
      ],
      "answer": 0,
      "difficulty": "easy",
      "explanation": "Over a long interval a client can only spend tokens as fast as they are added, so refill rate is the sustained throughput ceiling. Capacity governs short bursts above it."
    },
    {
      "q": "Which status code signals a rate-limited HTTP request?",
      "options": [
        "403, indicating the client's credentials were revoked",
        "503, indicating the entire service is currently unavailable",
        "429, optionally paired with Retry-After guidance",
        "409, indicating the request conflicts with another one"
      ],
      "answer": 2,
      "difficulty": "easy",
      "explanation": "429 Too Many Requests specifically means the client exceeded its quota and should retry later, which Retry-After quantifies. 403 implies retrying will never help."
    },
    {
      "q": "What is the leaky bucket's defining behavior?",
      "options": [
        "It permits unlimited instantaneous bursts as long as the daily total stays within quota",
        "Requests drain at a constant rate, smoothing bursts into a steady flow",
        "It multiplies each client's allowance whenever the system is idle",
        "It rejects all traffic for a cooldown period after any single breach"
      ],
      "answer": 1,
      "difficulty": "easy",
      "explanation": "The leaky bucket queues arrivals and releases them at a fixed drain rate, so downstream sees perfectly smooth output. Overflow beyond the queue is dropped."
    },
    {
      "q": "What is the classic flaw of a fixed window counter?",
      "options": [
        "Bursts at a window boundary can allow nearly double the intended rate",
        "It needs floating-point math, which drifts over long uptimes",
        "It cannot be implemented in Redis without server-side scripting",
        "Counters must be stored separately per URL path, which explodes memory usage quickly"
      ],
      "answer": 0,
      "difficulty": "easy",
      "explanation": "Because the counter resets abruptly, a client can spend its full quota just before the boundary and again just after, delivering 2x the limit within one rolling window."
    },
    {
      "q": "Why rate limit per API key or user rather than only globally?",
      "options": [
        "Global limits are prohibited by most cloud providers' API terms",
        "Per-key limits remove the need for authenticating requests entirely",
        "One noisy client can otherwise consume the shared quota and starve others",
        "Per-key limits make every response automatically cacheable at CDN edge locations"
      ],
      "answer": 2,
      "difficulty": "easy",
      "explanation": "A global cap protects the backend but distributes pain indiscriminately. Per-identity limits contain the blast radius so a single runaway client cannot degrade everyone else."
    },
    {
      "q": "What does the sliding window log approach store?",
      "options": [
        "One counter per client, decayed exponentially every second",
        "Timestamps of recent requests, pruned as they age out of the window",
        "A fixed boolean flag per window marking whether the client exceeded its quota there",
        "The average latency of each client's requests in the window"
      ],
      "answer": 1,
      "difficulty": "easy",
      "explanation": "Keeping every request timestamp lets the limiter count exactly how many fall inside the true rolling window, giving perfect accuracy at the cost of per-request memory."
    },
    {
      "q": "Where is a rate limiter most commonly enforced?",
      "options": [
        "Inside the database, by throttling slow queries per connection",
        "In the client SDK, which the server trusts to self-report its usage",
        "At the DNS layer, by delaying resolution for overly hot clients",
        "At the API gateway or edge, before requests reach backend services"
      ],
      "answer": 3,
      "difficulty": "easy",
      "explanation": "Rejecting at the gateway costs almost nothing and shields every downstream service. Client-side enforcement cannot be trusted because the client is exactly what you are limiting."
    },
    {
      "q": "Sliding window counter (two weighted windows) vs a full log: what do you give up?",
      "options": [
        "It rejects far more legitimate traffic than any other algorithm",
        "It requires synchronized clocks across every client device",
        "Exactness: it approximates the rolling count using far less memory",
        "It cannot express limits over any interval shorter than a full minute of wall time"
      ],
      "answer": 2,
      "difficulty": "medium",
      "explanation": "Interpolating the previous window's count assumes traffic was evenly spread there, so the estimate can be slightly off. In exchange you store two integers rather than every timestamp."
    },
    {
      "q": "A distributed limiter keeps per-node local counters with no synchronization. What is the effect?",
      "options": [
        "Each node rejects all traffic as soon as any node's counter fills",
        "The limit still gets enforced globally as long as the load balancer is strictly round-robin",
        "Requests queue at the load balancer until the counters synchronize",
        "A client can reach roughly N times the limit by spreading requests across nodes"
      ],
      "answer": 3,
      "difficulty": "medium",
      "explanation": "Each of the N nodes independently permits a full quota because none sees the others' traffic, so the effective global limit is N times the intended one."
    },
    {
      "q": "You centralize counters in Redis. What are the main new risks?",
      "options": [
        "Redis silently rounds all counters to powers of two, loosening every configured limit",
        "Added latency per request and a dependency that can fail or hotspot",
        "Counters can never expire, so memory grows without any bound",
        "Atomic increments become impossible, so counts are always stale"
      ],
      "answer": 1,
      "difficulty": "medium",
      "explanation": "Accuracy now costs a network round trip on every request, and the store becomes a shared critical dependency where a hot key or outage affects all traffic."
    },
    {
      "q": "Why prefer failing open (allowing traffic) when the limiter backend is down for a consumer API?",
      "options": [
        "Availability is preserved; the limiter is protection, not a correctness gate",
        "Failing open resets every client's counters, giving all clients a clean slate afterwards",
        "HTTP requires that requests be served whenever intermediaries fail",
        "Failing closed would corrupt the counter store permanently"
      ],
      "answer": 0,
      "difficulty": "medium",
      "explanation": "Failing closed turns a counter outage into a total service outage. Since limits guard against abuse rather than enforce correctness, briefly unmetered traffic is the smaller harm."
    },
    {
      "q": "A batch client sends 10k requests at midnight and gets throttled. Which limiter change lets it proceed smoothly?",
      "options": [
        "A fixed window aligned exactly to midnight so the burst fits inside it",
        "A larger bucket capacity allowing bursts while keeping the average rate",
        "A leaky bucket with a slower drain, spreading the burst across the day",
        "Per-IP limits, since batch jobs originate from many source addresses"
      ],
      "answer": 1,
      "difficulty": "medium",
      "explanation": "Token bucket separates burst from average: raising capacity lets an idle client cash in accumulated tokens at once while the refill rate still caps sustained usage."
    },
    {
      "q": "Clients retry immediately on 429, amplifying load. What server guidance most directly breaks the loop?",
      "options": [
        "Return 500 instead so clients switch over to their error path",
        "Blackhole the client's IP for an hour after the very first 429",
        "Serve cached stale responses to every throttled client instead",
        "Retry-After headers plus jittered exponential backoff in client guidance"
      ],
      "answer": 3,
      "difficulty": "medium",
      "explanation": "Retry-After tells clients when capacity actually returns, and exponential backoff with jitter keeps them from converging on that instant together."
    },
    {
      "q": "Why add jitter to client retry backoff?",
      "options": [
        "To desynchronize retries so they don't arrive in synchronized waves",
        "To make legitimate retry traffic harder for attackers to fingerprint and imitate",
        "To ensure each client's total wait time works out exactly equal",
        "To satisfy TCP congestion control requirements on retransmission"
      ],
      "answer": 0,
      "difficulty": "medium",
      "explanation": "Pure exponential backoff keeps clients that failed together retrying together, re-creating the spike. Randomizing the delay spreads them across the interval."
    },
    {
      "q": "Tiered limits (free vs paid). Where does the tier lookup belong?",
      "options": [
        "In the client, which selects its own tier via a request header",
        "In the database layer, throttling queries by table ownership",
        "In the gateway's auth step, attaching quota attributes to the verified identity",
        "In DNS, by resolving paying customers to a separate pool of faster endpoints"
      ],
      "answer": 2,
      "difficulty": "medium",
      "explanation": "Tier is a property of the authenticated principal, so resolving it during auth means the limiter uses a server-verified value that clients cannot forge, with no extra lookup."
    },
    {
      "q": "1M active clients with a 10 req/s limit each, sliding window log in Redis. Why does memory blow up?",
      "options": [
        "Storing per-request timestamps costs memory proportional to allowed requests per window per client",
        "Redis duplicates each key on every shard, squaring the total storage",
        "TTL metadata dominates, costing roughly a megabyte per client key",
        "Sorted sets must store the full request body alongside every entry"
      ],
      "answer": 0,
      "difficulty": "hard",
      "explanation": "The log keeps one sorted-set entry per request in the window, so total memory scales as clients times requests per window rather than one counter per client."
    },
    {
      "q": "Global limit of 1000 req/s across 10 gateway nodes: split local quotas vs a central counter?",
      "options": [
        "Split quotas overshoot the global limit whenever traffic is skewed toward one node",
        "Split quotas waste capacity under skewed traffic; central counters add latency and a failure point",
        "Central counters can never be made atomic across nodes, so they systematically undercount by design",
        "Split quotas require sticky sessions, which double memory usage on the balancer"
      ],
      "answer": 1,
      "difficulty": "hard",
      "explanation": "A static 100/s per node throttles a hot node while others sit idle, so the global limit is under-used. Central counting is accurate but pays a round trip and concentrates risk."
    },
    {
      "q": "A downstream handles 5k req/s. Steady traffic is 4k with 3x spikes lasting 10s. Which limiter config fits best?",
      "options": [
        "A fixed one-minute window of 300k requests aligned to the wall clock",
        "A token bucket refilling at 12k/s with capacity one, matching the spike rate",
        "A leaky bucket draining at 12k/s backed by an unbounded in-memory queue",
        "A token bucket refilling near 5k/s with burst capacity sized to downstream headroom"
      ],
      "answer": 3,
      "difficulty": "hard",
      "explanation": "The refill rate must match what downstream can actually sustain, around 5k/s, while a bounded burst absorbs short spikes. Refilling at 12k/s would simply pass the overload through."
    },
    {
      "q": "Thousands of users behind one corporate NAT constantly hit per-IP limits. What is the best fix?",
      "options": [
        "Raise the global limit for everyone until the complaints stop arriving across all users",
        "Switch to per-region limits derived from GeoIP lookups at the edge",
        "Key limits on authenticated identity where available, falling back to IP",
        "Exempt all known corporate IP ranges from rate limiting entirely"
      ],
      "answer": 2,
      "difficulty": "hard",
      "explanation": "IP is a poor proxy for a user once NAT collapses thousands behind one address. Keying on the authenticated principal restores fairness, with IP retained only for anonymous traffic."
    }
  ]
}
